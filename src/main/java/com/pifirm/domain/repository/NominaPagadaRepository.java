package com.pifirm.domain.repository;

import com.pifirm.persistence.entity.NominaPagadaEntity;
import org.springframework.data.jpa.repository.JpaRepository;

public interface NominaPagadaRepository extends JpaRepository<NominaPagadaEntity, Long> {
}
